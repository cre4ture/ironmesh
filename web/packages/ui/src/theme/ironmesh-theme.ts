import { createTheme } from "@mantine/core";

export const ironmeshTheme = createTheme({
  primaryColor: "teal",
  fontFamily: "Space Grotesk, system-ui, sans-serif",
  headings: {
    fontFamily: "Space Grotesk, system-ui, sans-serif"
  }
});
